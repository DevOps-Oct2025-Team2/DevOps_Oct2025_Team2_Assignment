name: Promote - staging to main (PR + approval + merge)

on:
  push:
    branches: ["staging"]

permissions:
  contents: write
  pull-requests: write

jobs:
  open-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create PR (staging -> main) if not exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          existing=$(gh pr list --base main --head staging --state open --json number --jq '.[0].number')
          if [ -n "$existing" ]; then
            echo "PR already exists: #$existing"
            exit 0
          fi

          gh pr create \
            --base main \
            --head staging \
            --title "Release staging -> main" \
            --body "Auto-generated after staging update. Requires approval to merge."

  approve-and-merge:
    needs: open-pr
    runs-on: ubuntu-latest

    #set required reviewers (Scrum Master)
    environment: production

    steps:
      - name: Merge PR (staging -> main)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pr=$(gh pr list --base main --head staging --state open --json number --jq '.[0].number')
          if [ -z "$pr" ]; then
            echo "No open PR from staging -> main"
            exit 0
          fi

          # Merge immediately after approval (your branch rules still protect main)
          gh pr merge "$pr" --merge --delete-branch=false
